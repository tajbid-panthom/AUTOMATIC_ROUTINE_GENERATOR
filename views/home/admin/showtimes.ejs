<!-- Assumes Bootstrap 5 CSS and Bootstrap Icons CSS are loaded -->
<% layout("/layouts/boilerplate.ejs") %>

<style>
  /* Custom styles */
  .content-box {
    border-radius: 0.85rem; /* Slightly more rounding for outer boxes */
    background-color: white;
    height: 100%; /* Make columns try to be same height */
  }
  .inner-pink-box {
    border-radius: 0.6rem; /* Rounding for inner pink boxes */
    background-color: #dc586d; /* Pinkish-red color */
    color: white;
  }
  .scrollable-list {
    /* Adjust height as needed, maybe make it relative or calculate */
    /* Let's use viewport height units for an example */
    max-height: calc(
      70vh - 100px
    ); /* Example: Calculate based on viewport, minus padding/margins */
    min-height: 200px; /* Ensure minimum height */
    overflow-y: auto;
    padding-right: 0.5rem; /* Space for scrollbar */
  }
  /* Optional: Scrollbar styling */
  .scrollable-list::-webkit-scrollbar {
    width: 8px;
  }
  .scrollable-list::-webkit-scrollbar-track {
    background: #f1f1f1;
    border-radius: 10px;
  }
  .scrollable-list::-webkit-scrollbar-thumb {
    background: #ccc;
    border-radius: 10px;
  }
  .scrollable-list::-webkit-scrollbar-thumb:hover {
    background: #bbb;
  }

  /* Small white button/badge for Day */
  .day-badge {
    background-color: white;
    color: #dc586d; /* Pink text */
    border-radius: 0.5rem;
    padding: 0.2rem 0.8rem;
    font-size: 0.9em;
    font-weight: bold;
    border: 1px solid #ddd; /* Optional subtle border */
  }
  /* Delete button */
  .btn-delete-time {
    background: none;
    border: none;
    color: white;
    padding: 0 0.3rem; /* Minimal padding */
    line-height: 1; /* Prevent extra height */
  }
  .btn-delete-time:hover {
    opacity: 0.8;
  }
</style>
<div class="main_body_color">
  <div class="container pt-5 pb-2">
    <h2
      class="text-center mb-1"
      style="
        color: #f8f9fa;
        font-weight: 500;
        font-size: 3rem;
        text-shadow: 1px 2px 4px rgba(0, 0, 0, 0.595);
      "
    >
      Add Preferred Time
    </h2>
    <h2
      class="text-center"
      style="
        color: #4c1d3d;
        font-weight: 500;
        font-size: 2.5rem;
        text-shadow: 1px 2px 4px rgba(255, 255, 255, 0.595);
      "
    >
      Sajib Chatterjee
    </h2>

    <div class="container py-2">
      <div class="row g-4 justify-content-center">
        <!-- g-4 adds gutters -->

        <!-- Left Column: Course List -->
        <div class="col-lg-7">
          <div class="content-box p-3 shadow-sm h-100">
            <!-- h-100 tries to match height if possible -->
            <div class="course-list-scroll">
              <!-- Course Item START (Repeat this block) -->
              <!-- In EJS: -->
              <div
                class="d-flex align-items-center p-3 rounded mb-3 inner-pink-box"
              >
                <i class="bi bi-mortarboard fs-3 me-3"></i>
                <!-- Academic Icon -->
                <div>
                  <span class="fw-bold d-block"
                    >Title : Algorithms Labratory</span
                  >
                  <span class="fw-bold d-block mt-1"
                    >Course Code : CSE 2202</span
                  >

                  <span class="fw-bold d-block mt-1">Course Credit : 1.5</span>
                </div>
                <!-- Delete button removed as it's not in this image -->
              </div>
              <!-- Course Item END -->
              <!-- In EJS:-->

              <!-- Static Example Items -->
              <div
                class="d-flex align-items-center p-3 rounded mb-3 inner-pink-box"
              >
                <i class="bi bi-mortarboard fs-3 me-3"></i>
                <div>
                  <span class="fw-bold d-block"
                    >Title : Algorithms Labratory</span
                  >
                  <span class="fw-bold d-block mt-1"
                    >Course Code : CSE 2202</span
                  >
                  <span class="fw-bold d-block mt-1">Course Credit : 1.5</span>
                </div>
              </div>
              <div
                class="d-flex align-items-center p-3 rounded mb-3 inner-pink-box"
              >
                <i class="bi bi-mortarboard fs-3 me-3"></i>
                <div>
                  <span class="fw-bold d-block"
                    >Title : Algorithms Labratory</span
                  >
                  <span class="fw-bold d-block mt-1"
                    >Course Code : CSE 2202</span
                  >

                  <span class="fw-bold d-block mt-1">Course Credit : 1.5</span>
                </div>
              </div>
              <!-- Add more static items if needed for testing scroll -->
            </div>
          </div>
        </div>
        <!-- /col-lg-7 -->

        <!-- Right Column: Preferred Times List -->
        <div class="col-lg-5 d-flex flex-column">
          <div class="content-box p-3 shadow-sm flex-grow-1">
            <div class="scrollable-list">
              <!-- Also make this scrollable -->

              <!-- Preferred Time Item START (Repeat this block) -->
              <!-- In EJS: -->
              <div
                class="d-flex align-items-center justify-content-between p-3 rounded mb-3 inner-pink-box"
              >
                <!-- Left Side -->
                <div class="d-flex align-items-center">
                  <i class="bi bi-clock-history fs-4 me-3"></i>
                  <!-- Clock Icon -->
                  <span class="day-badge me-3">Sunday</span>
                  <!-- Day Badge -->
                  <span class="fw-bold">Time :</span
                  ><span class="ms-1">9:00 AM - 1:00 PM</span>
                  <!-- Time Range -->
                </div>
                <!-- Right Side (Delete Button) -->
                <!-- Example form for delete -->
                <form
                  action="/preferred-time/delete/id?_method=DELETE"
                  method="POST"
                >
                  <button
                    type="submit"
                    class="btn-delete-time"
                    aria-label="Delete Time"
                  >
                    <i class="bi bi-x-circle-fill fs-5"></i>
                    <!-- Delete Icon -->
                  </button>
                </form>
              </div>
              <!-- Preferred Time Item END -->
              <!-- In EJS: -->

              <!-- Static Example Items -->
              <div
                class="d-flex align-items-center justify-content-between p-3 rounded mb-3 inner-pink-box"
              >
                <div class="d-flex align-items-center">
                  <i class="bi bi-clock-history fs-4 me-3"></i>
                  <span class="day-badge me-3">Sunday</span>
                  <span class="fw-bold">Time :</span
                  ><span class="ms-1">9:00 AM - 1:00 PM</span>
                </div>
                <form action="#" method="POST">
                  <button
                    type="button"
                    class="btn-delete-time"
                    aria-label="Delete Time"
                  >
                    <i class="bi bi-x-circle-fill fs-5"></i>
                  </button>
                </form>
              </div>
              <div
                class="d-flex align-items-center justify-content-between p-3 rounded mb-3 inner-pink-box"
              >
                <div class="d-flex align-items-center">
                  <i class="bi bi-clock-history fs-4 me-3"></i>
                  <span class="day-badge me-3">Sunday</span>
                  <span class="fw-bold">Time :</span
                  ><span class="ms-1">9:00 AM - 1:00 PM</span>
                </div>
                <form action="#" method="POST">
                  <button
                    type="button"
                    class="btn-delete-time"
                    aria-label="Delete Time"
                  >
                    <i class="bi bi-x-circle-fill fs-5"></i>
                  </button>
                </form>
              </div>
              <div
                class="d-flex align-items-center justify-content-between p-3 rounded mb-3 inner-pink-box"
              >
                <div class="d-flex align-items-center">
                  <i class="bi bi-clock-history fs-4 me-3"></i>
                  <span class="day-badge me-3">Sunday</span>
                  <span class="fw-bold">Time :</span
                  ><span class="ms-1">9:00 AM - 1:00 PM</span>
                </div>
                <form action="#" method="POST">
                  <button
                    type="button"
                    class="btn-delete-time"
                    aria-label="Delete Time"
                  >
                    <i class="bi bi-x-circle-fill fs-5"></i>
                  </button>
                </form>
              </div>
              <div
                class="d-flex align-items-center justify-content-between p-3 rounded mb-3 inner-pink-box"
              >
                <div class="d-flex align-items-center">
                  <i class="bi bi-clock-history fs-4 me-3"></i>
                  <span class="day-badge me-3">Sunday</span>
                  <span class="fw-bold">Time :</span
                  ><span class="ms-1">9:00 AM - 1:00 PM</span>
                </div>
                <form action="#" method="POST">
                  <button
                    type="button"
                    class="btn-delete-time"
                    aria-label="Delete Time"
                  >
                    <i class="bi bi-x-circle-fill fs-5"></i>
                  </button>
                </form>
              </div>
            </div>
            <!-- /scrollable-list -->
          </div>
          <!-- /content-box -->
        </div>
        <!-- /col-lg-5 -->
      </div>
      <!-- /row -->
    </div>
    <div class="d-flex justify-content-center align-items-center gap-3 p-4">
      <!-- Adjust gap-* for spacing, p-* for surrounding padding -->

      <!-- Add Preferred Time Button -->
      <a href="/add-time">
        <button
          type="button"
          class="btn btn-lg fw-bold text-white shadow-sm"
          style="
            background-color: #af40af;
            border-color: #af40af;
            border-radius: 0.75rem;
            padding: 0.6rem 1.5rem;
          "
        >
          Add Preferred Time
        </button></a
      >
      <form action="/download" method="POST">
        <!-- Submit Button -->
        <button
          type="submit"
          class="btn btn-lg fw-bold text-white shadow-sm"
          style="
            background-color: #0056b3;
            border-color: #0056b3;
            border-radius: 0.75rem;
            padding: 0.6rem 2rem;
          "
        >
          <!-- Using a darker blue - adjust hex code if needed, e.g., #0d6efd for standard BS blue -->
          Submit
        </button>
      </form>
    </div>
    <!-- /container -->
  </div>
</div>
